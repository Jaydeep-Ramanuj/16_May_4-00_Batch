#include <stdio.h>


void main(){

    int a=10;
    printf("Before increment a: %d",a);

    // Increment/decrement is unary operators.


    // Post increment / decrement, it will increment/decrement
    // after utilization of a variable

    // increment operator, it will increase variable with 1
    //a++;
    //printf("\na: %d",a);

    // decrement operator, it will decrease variable with 1
    //a--;
    //printf("\na: %d",a);


    //printf("\nAfter increment, a: %d",a++);
    //printf("\na: %d",a);


    // Pre increment / decrement, it will increment/decrement
    // before utilization of a variable

    //++a;
    //printf("\na: %d",a);

    //--a;
    //printf("\na: %d",a);


    printf("\na: %d",++a);

}
